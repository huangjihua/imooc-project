extends ../layout
block  content
    .container
        .row
        each cate in  categories
            .panel.panel-default
                .panel-heading
                    h4 #{cate.name}
                .panel-body
                    if cate.movie && cate.movie.length > 0
                        each item in cate.movie
